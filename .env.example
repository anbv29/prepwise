NODE_ENV=development
API_PORT=4000
WEB_ORIGIN=http://localhost:3000

# Added in later implementation steps.
MONGODB_URI=
MONGODB_DATABASE=interview_prep
SESSION_TTL_DAYS=7
SESSION_COOKIE_NAME=prep_session
LLM_API_KEY=
SEARCH_API_KEY=

# Localhost/private URLs are needed only for local development and evaluator fixtures.
ALLOW_PRIVATE_NETWORKS=true
